script: pileupExonFilter.py
usage: python pileupExonFilter.py bedfile in_pileup > out_pileup
function: passes lines in in_pileup which position is inside exon ranges which is specified by bedfile.
dependency: python RangeSet.py
#
script: RangeSet.py
usage: class file
function: create range set. create normalized range list internally, and allows to determine that requested position is insided or not as O(logn) speed.
dependency: python
#
script: fastqUnpairedFilter.py
usage: remove Unpaired read from pair fastq sequence files.
dependency: python
